// import { ImageLoader } from '../src/ImageLoader';

// describe('ImageLoader', () => {
//   describe('#loadImages', () => {
//     it('should call back when done.', (done) => {
//       var inputHash = {
//         image1:"bogusPath"
//       };

//       var loader = new ImageLoader();

//       loader.loadImages(
//         inputHash, 
//         (outputHash) => {
//           expect(outputHash.image1.complete).toBeTruthy();
//           done();
//         }
//       );

//     });
//   });
// });
